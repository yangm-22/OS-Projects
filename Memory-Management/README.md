# Project Description

This project simulates a Memory Management Unit (MMU) which translates logical addresses into physical addresses using a page table and Translation Lookaside Buffer (TLB), with a FIFO page replacement policy. 
